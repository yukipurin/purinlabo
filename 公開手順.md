# purinlabo.com を公開する手順

GitHub Pages で公開する（Cloudflareは使わない＝新規アカウント不要）。
自動デプロイの仕込みは `.github/workflows/deploy.yml` に入れてあるので、
下の2つだけ人がやれば動き出す。DNSの値は GitHub 公式ドキュメントで確認済み（2026-09-08）。

---

## 手順1：GitHub側の設定（1分）

1. https://github.com/yukipurin/purinlabo/settings/pages を開く
2. **Build and deployment** の **Source** を **「GitHub Actions」** に変更
   （初期値は「Deploy from a branch」。ここを変えないと動かない）
3. 下の **Custom domain** に `purinlabo.com` と入力して Save
   - 「DNS check in progress」と出るのは正常。手順2が終われば通る
4. DNSが通ったあと、**Enforce HTTPS** にチェックを入れる
   （証明書の発行に最大24時間かかる。チェックできない間は待つ）

## 手順2：お名前.comでDNSを設定（5分）

お名前.com Navi → **ドメイン** → **DNS** → 対象に `purinlabo.com` を選ぶ
→ **DNSレコード設定を利用する**

以下を1行ずつ追加する。

### Aレコード（4行）

| ホスト名 | TYPE | VALUE |
|---|---|---|
| （空欄） | A | `185.199.108.153` |
| （空欄） | A | `185.199.109.153` |
| （空欄） | A | `185.199.110.153` |
| （空欄） | A | `185.199.111.153` |

### CNAMEレコード（1行・www用）

| ホスト名 | TYPE | VALUE |
|---|---|---|
| `www` | CNAME | `yukipurin.github.io.` |

※ ホスト名が「空欄」＝ `purinlabo.com` そのものを指す。
※ CNAMEの値は末尾のドットまで入れる。
※ お名前.comが最初から入れている初期レコード（転送設定など）があれば削除する。
※ AAAAレコード（IPv6）は任意。入れるなら
　`2606:50c0:8000::153` / `2606:50c0:8001::153` / `2606:50c0:8002::153` / `2606:50c0:8003::153`

---

## 反映の確認

DNSは数十分〜最大24時間かかる。確認コマンド：

```bash
dig +short purinlabo.com
```

`185.199.10x.153` が返ってくれば通っている。

ブラウザで https://purinlabo.com が開けば完了。
以降は `git push` するたびに自動で公開される。

---

## 注意

- **アクセス解析は未設定。** Cloudflareを使わない構成にしたので、
  Cloudflare Web Analytics は入らない。主KPIの「検索クリック数」は
  Google Search Console で取れるので、まずはそれだけで足りる。
  必要になったら Cookieless の解析（GoatCounter等）を検討する。
- 既存の `yukipurin.github.io` はそのまま残る。別リポジトリなので影響しない。
