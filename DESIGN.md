# ぷりんラボ DESIGN.md

purinlabo.com の見た目の決まりごと。AIエージェントはこれを読んでUIを作る。
実装の規約は `CLAUDE.md`、文章の規約は `../文言プロンプト.md`。

## 全体の方針

**壮大でシンプル。** ただし壮大さは「大きな文字」からは来ない。
Apple / SpaceX / Bugatti / Nike / PlayStation を調べた結果、機構は次の4つだった。

1. **1帯＝1画面のフルブリード。** 節は画面幅いっぱいを占める
2. **背景色の変化そのものが節の区切り。** 罫線・カード・枠を使わない
3. **装飾をゼロにする。** Bugattiはアクセント色すら持たない
4. **余白は台座。** 見出しの上に十分な空きを取り、要素を密集させない

**one design language, different volumes**（Apple）に倣い、面ごとに音量を変える。

| 面 | 音量 | 作り |
|---|---|---|
| Home / About / お知らせ | 壮大 | フルブリード帯・装飾ゼロ・大きな余白 |
| ツールページ | 作業 | 実用寄り。手で描いた枠と紙の質感を残す |

## 色

素材は作業場から取る。**アクセントは1色だけ**（Ferrari「used scarcely」に倣い、控えめに使う）。

| トークン | 値 | 役割 |
|---|---|---|
| `--paper` | `#faf4e8` | クラフト紙。明るい帯の地 |
| `--paper-deep` | `#f2e8d6` | 一段濃い紙。帯を交互にするときの相方 |
| `--ink` | `#241d13` | 濃い帯の地。純黒にしない（紙の世界なので） |
| `--ink-2` | `#2f2618` | 本文の文字色（明るい帯の上） |
| `--on-ink` | `#f6f0e3` | 濃い帯の上の文字 |
| `--muted` | `#6f6353` | 補助テキスト（明るい帯） |
| `--muted-on-ink` | `#a2957f` | 補助テキスト（濃い帯） |
| `--kraft` | `#b87d45` | ダンボール。ラベルと罫のごく一部 |
| `--tape` | `#3d8b64` | **唯一のアクセント。** ボタンとリンクだけ |

**やらないこと：** グラデーション、影（ツールの作業面を除く）、2つ目のアクセント色。

## 文字

| 役割 | サイズ | 行間 | 字間 | 太さ |
|---|---|---|---|---|
| display-xl（Homeの見出し） | 60px | 1.15 | -0.01em | 700 |
| display-lg（帯の見出し） | 38px | 1.25 | -0.005em | 700 |
| display-md | 24px | 1.5 | 0 | 700 |
| lead | 18px | 1.95 | 0 | 400 |
| body | 16px | 1.95 | 0 | 400 |
| label | 12px | 1 | **0.24em** | 700 |

- 書体：見出し `ZenMaru`（自前配信）／本文はシステムのゴシック
- **ラベルは字間を大きく開ける。** 壮大側の署名（Bugatti 2-4px, SpaceX 1.6px に相当）
- 見出しは行間を詰める。display で 1.15〜1.25

## レイアウト

- **基準単位 8px。** 帯の上下は 112px（狭い画面では 72px）
- 帯は `width: 100%`。中の読み幅は最大 940px
- **帯と帯の間に罫線を引かない。** 背景色が変わることが区切り
- 要素を密集させない。見出しと本文の間は 24px 以上

## 部品

- **ボタン：** `--tape` の塗り1種のみ。角丸4px。影なし
- **リンク：** `--tape` の文字色＋下線
- **カードを作らない。** 一覧は縦に積み、間隔で分ける
- **判子・傾き・破線・手描きの枠は Home 側では使わない**（ツールページのみ）

## ツールページだけの例外

道具は道具らしく、実用に寄せる。紙の質感、手で描いた枠（`.hand-box` `.hand-dash`）、
情報密度の高い設定欄を使ってよい。Home の静けさとは別の音量で鳴らす。
